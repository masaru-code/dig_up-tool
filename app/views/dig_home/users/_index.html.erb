<div class="kaminari"> <%= paginate @users %> </div>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <div class= "gravatar"> <%= gravatar_for user, size: 50 %>
        <%= user.name %>
      </div>
    </li>
  <% end %>
</ul>