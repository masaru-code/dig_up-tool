<div class="kaminari"> <%= paginate @users %> </div>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <div class= "sinup-name">
        &emsp;<%= gravatar_for user, size: 50 %>&emsp;&emsp;<%= user.name %>
      </div>
    </li>
  <% end %>
</ul>